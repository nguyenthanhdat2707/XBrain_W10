apiVersion: v1
kind: Pod
metadata:
  name: payments-invalid-root-user
  namespace: payments
spec:
  securityContext:
    runAsUser: 0
  containers:
  - name: api
    image: ghcr.io/nguyenthanhdat2707/w10-api:0.0.2
    command: ["sh", "-c", "sleep 3600"]
    resources:
      limits:
        cpu: 100m
        memory: 64Mi

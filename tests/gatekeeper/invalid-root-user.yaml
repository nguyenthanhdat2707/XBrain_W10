apiVersion: v1
kind: Pod
metadata:
  name: invalid-root-user
  namespace: demo
spec:
  securityContext:
    runAsUser: 0
  containers:
  - name: busybox
    image: busybox:1.36.1
    command: ["sh", "-c", "sleep 3600"]
    resources:
      limits:
        cpu: 100m
        memory: 64Mi

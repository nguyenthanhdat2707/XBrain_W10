apiVersion: apps/v1
kind: Deployment
metadata:
  name: valid-replicas
  namespace: demo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: valid-replicas
  template:
    metadata:
      labels:
        app: valid-replicas
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
      containers:
      - name: busybox
        image: busybox:1.36.1
        command: ["sh", "-c", "sleep 3600"]
        resources:
          limits:
            cpu: 100m
            memory: 64Mi
